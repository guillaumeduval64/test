$(document).ready(function() {
var test;
var nbProductionObj = {% if nbProductionObj is defined %} {{ nbProductionObj }} {% endif %}; 
var items = document.getElementsByClassName('currentMatch');

    for (var i = 0; i < items.length; i++){
        test = items[i].innerText;
    var array=[];
    array.push(test);
alert(array);
if (test > nbProductionObj) {
	newColor = '#E9E581';

} else {
    newColor = "black";
    
}
items[i].style.color = newColor;;
alert(items[i].style.color);
}

});
